定义 是否是质数（被判断的数）：
    如果 被判断的数 小于 2：
        返回 假
    迭代 数1 在 范围（2，整数（被判断的数 取幂 0.5）加 1）：
        如果 被判断的数 取余 数1 等于 0：
            返回 假
    返回 真

定义 主函数（）：
    迭代 数字 在 范围（1，20）：
        如果 是否是质数（数字）：
            打印（数字）

    布尔值1 赋值为 1 大于 2 和 1 小于 3
    布尔值2 赋值为 1 大于 2 或 1 小于 3
    
    打印（布尔值1）
    打印（布尔值2）
    
主函数（）